model:
  name: "{{MODEL_NAME}}"
  model_type: "anomaly"
  framework: "xgboost"
  feature_group: "merchant_transactions"

guardrails:
  max_inference_latency_ms: 200
  data_access_scope: "same-merchant-only"
  pii_fields: []
  pii_handling: "mask"

deployment:
  endpoint_type: "serverless"
  auto_scaling: true
  canary_percentage: 10
